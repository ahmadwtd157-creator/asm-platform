services:
  backend:
    build: ./backend
    container_name: asm-backend
    ports: 
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: asm-frontend
    ports:
      - "3000:3000"
    restart: unless-stopped